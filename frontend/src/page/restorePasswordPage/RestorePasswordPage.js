import {RestorePassword} from "../../component";

const RestorePasswordPage = () => {
  return (
      <div className={'restore-password-page'}>
        <RestorePassword/>
      </div>
  );
};

export {RestorePasswordPage}
