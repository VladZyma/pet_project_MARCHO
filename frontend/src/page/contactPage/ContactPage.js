import {Contact, Top} from "../../component";

const ContactPage = () => {

  return (
      <section className={'contact-page'}>
        <Top title={'contact'}/>
        <Contact/>
      </section>
  );
};

export {ContactPage}