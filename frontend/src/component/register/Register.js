import {Top ,RegisterForm} from "../../component";

const Register = () => {

    return (
        <div className={'register'}>
            <Top title={'Register'}/>
            <RegisterForm/>
        </div>
    );
};

export {Register}